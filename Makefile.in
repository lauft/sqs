PREFIX=@prefix@
BINDIR=@bindir@
VARDIR=@vardir@

default:
	@echo "Run 'configure' and then 'make install'"

install:
	@mkdir -p $(VARDIR)
	@cp sqs        $(BINDIR)/.	
	@cp sqslock    $(BINDIR)/.
	@cp sqsunlock  $(BINDIR)/.
	@cp sqsrunner  $(BINDIR)/.
	@cp sqsconfig  $(BINDIR)/.
